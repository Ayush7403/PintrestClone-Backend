<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || "Pinterest Clone" %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 text-gray-800">
  <header class="bg-white shadow-md sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 py-3 flex justify-between items-center">
      <a href="/" class="text-xl font-bold text-red-600">Pintrest</a>
      <nav class="space-x-4 text-sm hidden sm:block">
        <a href="/feed" class="hover:text-red-600">Feed</a>
        <a href="/profile" class="hover:text-red-600">Profile</a>
        <a href="/add" class="hover:text-red-600">Add Post</a>
        <a href="/logout" class="hover:text-red-600">Logout</a>
      </nav>
      <button class="sm:hidden text-gray-700" onclick="toggleMenu()">☰</button>
    </div>
    <div id="mobileMenu" class="sm:hidden hidden px-4 pb-2">
      <a href="/feed" class="block py-1">Feed</a>
      <a href="/profile" class="block py-1">Profile</a>
      <a href="/add" class="block py-1">Add Post</a>
      <a href="/logout" class="block py-1">Logout</a>
    </div>
  </header>

  <script>
    function toggleMenu() {
      const menu = document.getElementById("mobileMenu");
      menu.classList.toggle("hidden");
    }
  </script>

    